#include <iostream>
using namespace std;

int main() {
    
    int width, height;

    cout << "너비를 입력하세요>>";
    cin >> width >> height; // 두 가지 변수를 입력 순서대로 저장
    cout << width << '\n' << height << '\n';

    /*
        " cout <<  ~~ " 입력받는 문장을 출력
        "cin >> a" 미리 만들어둔 변수 a에 방금 입력한 값을 저장
    */
}